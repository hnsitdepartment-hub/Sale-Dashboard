# Streamlit Cloud Database Configuration
# This file contains sensitive database credentials
# DO NOT commit this file to GitHub with real credentials!

[database]
# Candelahns Database (Main Sales Database)
candelahns_server = "103.86.55.183,2001"
candelahns_db = "Candelahns"
candelahns_user = "ReadOnlyUser"
candelahns_password = "902729@Rafy"

# KDS Database (Configuration Database)
kds_server = "localhost"
kds_db = "KDS_DB"
kds_user = "sa"
kds_password = "your_secure_password"

# Fresh Pick Database
fresh_pick_server = "103.86.55.183,2002"
fresh_pick_db = "CandelaFP"
fresh_pick_user = "ReadOnlyUser"
fresh_pick_password = "902729@Rafy"

# API Proxy Configuration (for cloud deployment)
# Set DB_API_PROXY environment variable to use API proxy instead of direct connections
# Example: DB_API_PROXY="https://your-api-proxy.ngrok.io"
api_proxy_url = ""

# Environment Settings
environment = "cloud"  # or "local"

[streamlit_cloud]
# Streamlit Cloud specific settings
enable_cloud_features = true
debug_mode = false

# Security Settings
allow_direct_connections = false  # Set to true only for testing
use_api_proxy = true  # Recommended for production
